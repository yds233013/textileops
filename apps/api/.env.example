
# Pilot mode. TextileOps still ingests, reconciles, calculates, detects and
# proposes; it stops changing anything by itself. A supplier's email will not
# move a delivery date on its own — it becomes something an operator confirms —
# and no action executes without a named human approval behind it. Turn this on
# for first contact with a real business's data.
PILOT_MODE=false
